<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Go Live on STIVE</title>
    <%- include("partials/headerLinks.ejs") %>
  </head>
  <body>
    <nav class="go-live-nav">
      <div class="go-live-nav-logo">
        <img src="/img/Logo.svg" alt="" />
      </div>
    </nav>
    <main class="before-live-main">
      <div class="before-live-form-container">
        <form
          action="/liveInfo/<%=id%>?meetingId=<%=meetingId%>&roomName=<%=roomName%>"
          method="POST"
          enctype="multipart/form-data"
        >
          <div class="form-field">
            <label for="title">Title</label>
            <input type="text" name="title" required placeholder="Title" />
          </div>
          <div class="form-field">
            <label for="description">Description</label>
            <input
              type="text"
              name="description"
              required
              placeholder="Desciption"
            />
          </div>
          <div class="form-field">
            <label for="thumbnail"> Thumbnail </label>
            <input type="file" name="thumbnail" required />
          </div>
          <div class="form-field">
            <label for="category">Category</label>
            <select name="category" id="category">
              <option value="" default disabled>Select a category</option>
              <option value="Entertainment">Entertainment</option>
              <option value="Gaming">Gaming</option>
              <option value="Education">Education</option>
              <option value="Travel">Travel</option>
            </select>
          </div>
          <div class="form-field">
            <label for="amount"> Amount</label>
            <input
              type="number"
              placeholder="Amount for this stream"
              name="amount"
              required
              min="30"
              max="500"
            />
          </div>
          <div class="form-field">
            <label for="paymentButton"
              >Paste Payment Button Code from Razorpay (Most pereferably)</label
            >
            <textarea
              class="payment-btn-code"
              name="paymentButton"
              id="paymentButton"
              cols="30"
              rows="3"
            ></textarea>
          </div>
          <div class="live-stream-link">
            ADD THIS LINK AS REDIRECT URL ON THE PAYMENT BUTTON
            <p>
              http://localhost/participant/<%=id%>?meetingId=<%=meetingId%>&roomName=<%=roomName%>
            </p>
          </div>
          <div class="go-live-btns">
            <button
              type="button"
              onclick="window.history.back();"
              class="btn btn-small btn-secondary"
            >
              Cancel
            </button>
            <button type="submit" class="btn btn-small btn-primary go-live-btn">
              Go Live
            </button>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
